{
  "routes": [
    {
      "route": "/leaderboard",
      "rewrite": "/leaderboard.html"
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/images/*.{png,jpg,gif,ico}", "/*.{css,scss,js}"]
  }
}